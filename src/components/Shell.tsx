import Link from "next/link";
type Item={href:string;label:string};
const admin:Item[]=[{href:"/admin",label:"⌂  Resumen"},{href:"/admin/batches",label:"◇  Lotes"},{href:"/admin/certificates",label:"▣  Certificados"},{href:"/admin/inventory",label:"▤  Inventario"},{href:"/admin/users-kyc",label:"♙  Usuarios y KYC"},{href:"/admin/blockchain",label:"⌁  Blockchain"}];
const company:Item[]=[{href:"/company",label:"⌂  Resumen ESG"},{href:"/company/certificates",label:"▣  Certificados"},{href:"/company/purchases",label:"⇄  Compras"},{href:"/company/traceability",label:"⌁  Trazabilidad"}];
export function Shell({role,children}:{role:"admin"|"company";children:React.ReactNode}){const items=role==="admin"?admin:company;const name=role==="admin"?"Livora Operaciones":"Coca-Cola Perú";return <div className="shell"><aside className="sidebar"><Link className="brand" href={role==="admin"?"/admin":"/company"}><span className="brandmark"/>Livora</Link><div className="workspace"><small>Espacio de trabajo</small><strong>{name}⌄</strong></div><nav className="nav">{items.map(i=><Link key={i.href} href={i.href}>{i.label}</Link>)}</nav><div className="sidebar-foot"><div className="user"><span className="avatar">{role==="admin"?"LM":"CC"}</span><div><strong>{role==="admin"?"Lucía Mendoza":"Equipo ESG"}</strong><small>{role==="admin"?"Administradora":"empresa@cocacola.pe"}</small></div></div><Link href="/" className="muted" style={{fontSize:11,display:"block",marginTop:14}}>← Cambiar perfil</Link></div></aside><main className="main"><header className="topbar"><span className="eyebrow">{role==="admin"?"Control de operaciones":"Portal corporativo"}</span><div className="top-actions"><span className="live">Red operativa</span><button className="btn ghost">◉</button></div></header><div className="content">{children}</div></main><nav className="mobile-nav">{items.slice(0,4).map(i=><Link key={i.href} href={i.href}>{i.label.replace(/^\S+\s+/,"")}</Link>)}</nav></div>}
export function PageHead({eyebrow,title,description,action}:{eyebrow:string;title:string;description:string;action?:React.ReactNode}){return <div className="page-head"><div><span className="eyebrow">{eyebrow}</span><h1>{title}</h1><p>{description}</p></div>{action}</div>}
export function Kpi({label,value,trend,accent}:{label:string;value:string;trend:string;accent?:string}){return <div className="card kpi" style={{"--accent":accent} as React.CSSProperties}><div className="kpi-label">{label}</div><div className="kpi-value">{value}</div><div className="trend">{trend}</div></div>}
export function Status({value}:{value:string}){return <span className={`status ${value}`}>{value.replaceAll("_"," ")}</span>}
