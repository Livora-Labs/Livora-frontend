import{PageHead}from"@/components/Shell";import{BatchCard}from"@/components/Assets";import{batches}from"@/lib/data";
export default function Batches(){return <><PageHead eyebrow="Activos trazables" title="Explorador de lotes" description="Sigue cada lote desde la recolección hasta su registro verificable en blockchain." action={<button className="btn primary">+ Registrar lote</button>}/><div className="toolbar"><div className="search"><input placeholder="Buscar por ID, recolector o centro..."/></div><select defaultValue="all"><option value="all">Todos los estados</option><option>RECEIVED</option><option>PROCESSING</option><option>IN_TRANSIT</option></select><select><option>Más recientes</option><option>Mayor peso</option></select></div><p className="muted" style={{fontSize:12}}>{batches.length} lotes encontrados</p><div className="grid collection-grid">{batches.map(b=><BatchCard batch={b} key={b.id}/>)}</div></>}
