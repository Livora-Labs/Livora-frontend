import Link from"next/link";import{notFound}from"next/navigation";import{PageHead,Status}from"@/components/Shell";import{batches,date,kg,shortId}from"@/lib/data";
export default async function Detail({params}:{params:Promise<{id:string}>}){const{id}=await params;const b=batches.find(x=>x.id===id);if(!b)notFound();const weight=Object.values(b.materialsActual||{}).reduce((a,c)=>a+c,0);return <><PageHead eyebrow="Lote verificable" title={`Lote #${shortId(b.id)}`} description="Registro de origen, composición y validación del material." action={<Status value={b.status}/>}/><div className="grid detail-grid"><section className="card" style={{padding:0,overflow:"hidden"}}><div className="hero-art"/><div style={{padding:20}}><div className="section-title"><h2>Composición</h2><strong>{weight?kg(weight):"Por pesar"}</strong></div>{Object.entries(b.materialsActual||{}).map(([m,v])=><div className="data-row" key={m}><span>{m}</span><strong>{kg(v)}</strong></div>)}</div></section><section className="detail-panel"><div className="card"><span className="eyebrow">Identidad del activo</span><h1>{Object.keys(b.materialsActual||{}).join(" + ")||"Lote en tránsito"}</h1><div className="chips"><span className="chip">◈ {b.requests.length} recolecciones</span><span className="chip">✓ Origen verificado</span><span className="chip">⌁ Arbitrum Sepolia</span></div><div className="data-list"><div className="data-row"><span>ID del lote</span><strong className="mono">{b.id}</strong></div><div className="data-row"><span>Recolector</span><strong>{b.collector.email}</strong></div><div className="data-row"><span>Centro de destino</span><strong>{b.destinationCenter?.email||"Pendiente"}</strong></div><div className="data-row"><span>Creado</span><strong>{date(b.createdAt)}</strong></div></div></div><div className="card" style={{marginTop:16}}><div className="section-title"><h2>Cadena de custodia</h2><span className="live">Verificada</span></div><div className="trace">{["Creado","Recolectado","En tránsito","Pesado","Registrado"].map((x,i)=><div className="trace-step" key={x}><strong>{x}</strong><span>{i<4?date(b.updatedAt):b.trace?"On-chain":"Pendiente"}</span></div>)}</div></div>{b.trace&&<div className="card" style={{marginTop:16}}><div className="section-title"><h2>Prueba blockchain</h2><span style={{color:"var(--green)"}}>✓ Confirmada</span></div><div className="data-row"><span>IPFS CID</span><strong className="mono">{shortId(b.trace.ipfsCid)}</strong></div><div className="data-row"><span>Transacción</span><strong className="mono">{shortId(b.trace.txHash)}</strong></div><div style={{display:"flex",gap:10,marginTop:16}}><a className="btn" href={`https://ipfs.io/ipfs/${b.trace.ipfsCid}`} target="_blank">Ver manifiesto ↗</a><a className="btn primary" href={`https://sepolia.arbiscan.io/tx/${b.trace.txHash}`} target="_blank">Ver en Arbiscan ↗</a></div></div>}<div className="card" style={{marginTop:16}}><div className="section-title"><h2>Evidencias de recolección</h2><span className="muted">{b.requests.length} registros</span></div>{b.requests.length?b.requests.map(r=><div className="network-row" key={r.id}><div><strong>{r.description}</strong><div className="muted" style={{fontSize:11,marginTop:5}}>{r.latitude}, {r.longitude} · Hogar #{r.householdId.slice(0,6)}</div></div><Status value={r.status}/></div>):<p className="muted">Las evidencias originales pertenecen al lote consolidado.</p>}</div></section></div><Link href="/admin/batches" className="btn" style={{display:"inline-block",marginTop:18}}>← Volver al explorador</Link></>}
